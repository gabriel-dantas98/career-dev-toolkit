# CareerOS Remotion demo

From `demo/remotion`, run `npm install`, optionally run `npm run browser:ensure` if Remotion has not downloaded its browser, then run `npm run render`; the 36-second 1920×1080 `CareerOSDemo` composition is written to `out/careeros-demo.mp4`.
